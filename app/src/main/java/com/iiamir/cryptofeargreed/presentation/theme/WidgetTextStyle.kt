package com.iiamir.cryptofeargreed.presentation.theme

import androidx.compose.ui.graphics.Color
import androidx.glance.text.FontWeight
import androidx.glance.text.TextStyle
import androidx.glance.unit.ColorProvider

val ToolbarAppNameTextStyle = TextStyle(
    fontSize = FOURTEEN_SP,
    color = ColorProvider(Color.White),
    fontWeight = FontWeight.Bold,
)

val NowFearNGreedIndexTextStyle = TextStyle(
    fontWeight = FontWeight.Bold,
    fontSize = EIGHTEEN_SP,
    color = ColorProvider(Color.White)
)

val NowFearNGreedIndexDateTextStyle = TextStyle(
    fontSize = FOURTEEN_SP,
    fontWeight = FontWeight.Bold,
    color = ColorProvider(Color.White)
)

val FearNGreedIndexDateTextStyle = TextStyle(
    fontSize = SIXTEEN_SP,
    color = ColorProvider(Color.White)
)

val FearNGreedIndexClassificationTextStyle = TextStyle(
    fontSize = SIXTEEN_SP,
    fontWeight = FontWeight.Bold,
)

val FearNGreedIndexTextStyle = TextStyle(
    fontSize = SIXTEEN_SP,
    fontWeight = FontWeight.Bold,
    color = ColorProvider(Color.White)
)

val ErrorContentTextStyle = TextStyle(
    fontSize = SIXTEEN_SP,
    color = ColorProvider(Color.Red)
)